source "https://rubygems.org"

# Use GitHub Pages gem (this locks Jekyll + plugins to the same versions GitHub runs)
gem "github-pages", group: :jekyll_plugins

# Optional, but common
gem "webrick"   # needed if you’re on Ruby 3.x
